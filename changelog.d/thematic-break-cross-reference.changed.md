- `assemble-news.sh` and `strip-non-invoking-markup.sh`: document candidate population divergence between `is_thematic_break` implementations (#747).
  Cross-references the two scripts and notes why underscore breaks are
  reachable only in `strip-non-invoking-markup.sh`.
