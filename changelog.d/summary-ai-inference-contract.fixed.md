- **`summary.yml` again uses an inference action that supports its configurable
  endpoint contract** (#769).
  Inference outages now emit a warning and leave the issue unchanged instead of
  failing the issue-open workflow.
