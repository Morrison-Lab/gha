- **Adopt `open-failure-issue` in `check-links` and `website-publish`** (#327). `check-links.yml` now calls `.github/actions/open-failure-issue@v2` instead of an inline `gh issue create` block (deduplicating repeat failure reports and handling missing labels gracefully), and `website-publish.yml` adds a `report-failure` job that files/appends to a tracking issue when site deployment fails on `main`.
