- `CLAUDE.md`: document `check_list_item_splices.mjs` and `check_table_splits.mjs` (#754).
  Documents the list-item merge splice rule, its wrapped-bullet trigger,
  house style requirements, and test suites.
- `lint-markdown/_pathspec.mjs`: normalize path separators in `isIgnored`
  for cross-platform Windows path matching.
