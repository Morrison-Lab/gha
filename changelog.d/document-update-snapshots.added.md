- **`update-snapshots` is now documented on the website** (#271). A new
  reference page covers the inputs, permissions, and example, plus a
  "When are the updated snapshots checked?" section: the workflow only
  verifies the suite passes against the accepted snapshots, so their
  correctness is judged at PR review of the pushed snapshot commit. Also
  adds the previously missing rows in the `README.md` and
  `website/workflows.qmd` catalog tables, the sidebar entry, and the
  per-workflow permissions bullets.
