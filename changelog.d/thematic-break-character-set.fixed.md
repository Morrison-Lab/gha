- `assemble-news.sh`: support underscore thematic breaks in `is_thematic_break` (#747).
  Aligns the recognized thematic break character set (`-`, `*`, `_`)
  with CommonMark 0.31.2 and sibling `strip-non-invoking-markup.sh`.
