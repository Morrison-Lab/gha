- **`preview`: visually mark deletions with `w:del` / `w:delText` in DOCX tracked changes**
  ([#797](https://github.com/Morrison-Lab/gha/issues/797)).
  Deleted paragraphs and replaced text now carry Word `w:del` and `w:delText` revision markup
  so removed content displays with visual strikethrough in Word.
