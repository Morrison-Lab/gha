# Draft-review override verification

This is a **throwaway** file used to verify the fix from #141: an explicit
`@claude review` on a *draft* PR should now produce a real review instead of
the "currently a draft … I will not proceed" refusal.

This file and the PR carrying it will be closed/deleted once verification is
complete. It is intentionally trivial so the reviewer has a small, real diff to
comment on.
