% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/added_lines.R
\name{parse_diff_added}
\alias{parse_diff_added}
\title{Line numbers a unified diff adds}
\usage{
parse_diff_added(diff)
}
\arguments{
\item{diff}{Character vector of unified-diff lines, or a single string
containing newlines (as the GitHub "list PR files" API returns in its
\code{patch} field).}
}
\value{
A named list mapping each new-file path to an integer vector of the
line numbers the diff adds. Empty list for a \code{NULL}/\code{NA}/empty input.
}
\description{
Parse unified-diff text into the new-file line numbers it adds or modifies.
}
\details{
Every line type must be handled, not only \code{+}. A context line advances the
new-file counter, a deleted line does not, and a \verb{\\ No newline at end of file} marker is not a line at all. Omitting the context branch silently
misnumbers every added line after the first context line -- a defect that
stays invisible when the diff was produced with \code{--unified=0}, since that
emits no context lines, and appears the moment anyone widens the context.
}
\examples{
parse_diff_added(c(
  "+++ b/x.qmd", "@ -1,2 +1,3 @", " context", "+added", "-gone", "+more"
))
}
