# Copy to .github/workflows/check-non-standard-chars.yml in your repo.
name: Check Non-Standard Characters

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  check:
    uses: Morrison-Lab/gha/.github/workflows/check-non-standard-chars.yml@v2
